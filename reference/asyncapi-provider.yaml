asyncapi: 3.0.0
info:
  title: ExAP Provider Event Stream
  version: 0.2.0-draft
channels:
  attentionEvents:
    address: exap.attention.events
    messages:
      attentionEvent:
        $ref: '#/components/messages/attentionEvent'
operations:
  receiveAttentionEvents:
    action: receive
    channel:
      $ref: '#/channels/attentionEvents'
components:
  messages:
    attentionEvent:
      name: attentionEvent
      title: ExAP Attention Event
      contentType: application/json
      payload:
        $ref: '../schemas/exap-attention-event.schema.json'
