# ExAP C4 Interoperability Scenarios

This directory contains cross-binding transcript scenarios for C4 checks. Each scenario declares a kind, binding list, and normalized fields that must remain present and equivalent across bindings.

Run:

```bash
python tests/interoperability/run_c4.py --scenarios tests/interoperability/scenarios --report tests/interoperability/reports/c4-report.md
```
