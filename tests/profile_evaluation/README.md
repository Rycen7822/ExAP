# ExAP C3 Profile Evaluation Suite

This suite checks profile transcript fixtures for effective Contract validation, capability compatibility, expected Attention Events, suppressed/internal records, state transitions, delivery reports, and coverage matrix entries.

Run:

```bash
python tests/profile_evaluation/run_c3.py --fixtures tests/fixtures/profile-transcripts --report tests/profile_evaluation/reports/c3-report.md
```
